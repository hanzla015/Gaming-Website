let again = new Typed('#comment', {
    strings: [
        "`Type your comment here.`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder",
    showCursor: false,
});