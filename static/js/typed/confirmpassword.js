let again = new Typed('#again', {
    strings: [
        "`Confirm your password`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder"


});