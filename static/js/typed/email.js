let emailtype = new Typed('#emailtype', {
    strings: [
        "`Enter your email address`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder"
});