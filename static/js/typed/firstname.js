let first_name = new Typed('#firstname', {
    strings: [
        "`Enter first name`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder"
});