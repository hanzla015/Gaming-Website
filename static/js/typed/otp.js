let otp_typed = new Typed('#otp_typed', {
    strings: [
        "`Enter the OTP`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder"
});