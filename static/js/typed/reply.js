let reply = new Typed('#reply', {
    strings: [
        "`Type your reply here.`"],
    backSpeed:500,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder",
    showCursor: false,
});