let username = new Typed('#username', {
    strings: [
        "`Enter username here`"],
    backSpeed:1000,
    typeSpeed:50,
    startDelay:500,
    backDelay:2000,
    loop:true,
    attr:"placeholder"
});